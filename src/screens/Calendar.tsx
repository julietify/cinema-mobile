import React, { FC } from "react"
import { Text, View } from "react-native"

export const Calendar: FC = () => {  
  return (
    <View>
      <Text>
        Calendar screen
      </Text>
    </View>
  )
}