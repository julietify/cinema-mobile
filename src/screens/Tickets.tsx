import React, { FC } from "react"
import { Text, View } from "react-native"

export const Tickets: FC = () => {
  return (
    <View>
      <Text>
        Tickets screen
      </Text>
    </View>
  )
}