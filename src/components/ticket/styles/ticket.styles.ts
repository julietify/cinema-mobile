import { StyleSheet } from "react-native"

export const typography = StyleSheet.create({
  text: {
    fontWeight: '700',
    fontSize: 12,
    marginTop: 10
  },
  title: {
    color: 'rgba(0, 0, 0, .3)',
    fontWeight: 'bold',
    fontSize: 12
  }
})